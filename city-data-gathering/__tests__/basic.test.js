describe('Basic test', () => {
    it('tests positive', () => {
        expect(2).toBe(2);
    });
});

describe('Basic test set', () => {
    it('tests positive', () => {
        expect(1+1).toBe(2);
    });
    it('tests negative', () => {
        expect(1+2).toBe(3);
    });
});
